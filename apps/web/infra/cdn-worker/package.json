{
  "name": "winlab-cdn-worker",
  "private": true,
  "version": "0.0.1",
  "description": "Cloudflare Worker that proxies Supabase Storage reads with edge caching + hotlink protection.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250909.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
